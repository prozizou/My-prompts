{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && (auth.token.email === 'abdoulbilalysy@gmail.com' || auth.token.email === 'amadousow413@gmail.com')",
        ".write": "auth != null && (auth.token.email === 'abdoulbilalysy@gmail.com' || auth.token.email === 'amadousow413@gmail.com')",
        "prompts": {
          "$promptId": {
            ".validate": "newData.hasChildren(['title','content','category','favorite','createdAt','updatedAt']) || !newData.exists()"
          }
        }
      }
    },
    ".read": false,
    ".write": false
  }
}
